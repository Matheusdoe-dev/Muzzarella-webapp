import { createGlobalStyle } from "styled-components";

const Fonts = createGlobalStyle`
    :root {
        --font-primary: 'Chelsea Market', cursive;
        --font-secondary: 'Lato', Arial, Helvetica, sans-serif;
    }
`;

export default Fonts;
